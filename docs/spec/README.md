# Moved

This directory previously mirrored the published spec pages and drifted from
them. The single source is now [`public/spec/`](../../public/spec/), served at
<https://spec.servicialo.com>.

CI (`scripts/verify-doc-claims.mjs`) fails if content other than this stub
reappears here.
